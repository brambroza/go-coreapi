-- public to docker 
    -- command palette  
        -  docker : add docker file to workspace 
           -- .net core  - linux  - port  - yes

-- command  
    -- :docker-compose up -d 


   
    echo "# go-coreapi" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/brambroza/go-coreapi.git
git push -u origin main

git remote add origin https://github.com/brambroza/go-coreapi.git
git branch -M main
git push -u origin main